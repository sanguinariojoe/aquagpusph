<?xml version="1.0" ?>

<!-- MLS.xml
Compute the MLS transformation matrix \f$ L_i \f$.

Such transformation matrix can be multiplied by the kernel gradient to produce
a new kernel gradient,
\f$ \nabla W^{L}_{ij} = L_i \cdot \nabla W_{ij} \f$, such that the lienar fields
differential operators are consistently computed

This tool is just computing the kernel transforamtion matrix. The usage of such
matrix is responsibility of the user.

To use it, just include this file. If you include this file several times, the
effect will be override (and computed just once). Therefore, please don't
include this preset with a prefix.

The output of this tool is the mls_fluid variable, not the mls one (which can
be overwritten by other tools)

This tool is prepared to work even if it is included more than once. However,
the first time the tool is included, 2 warnings will be reported due to the
impossibility of removing MLS_imove1 and MLS_mls_fluid. Just ignore them.
-->

<sphInput>
    <Variables>
        <Variable name="lambda" type="float*" length="N" />
		<Variable name="dshepard" type="vec*" length="N" />
		<Variable name="inormal" type="vec*" length="N" />
		<Variable name="frees" type="int*" length="N" />
		<Variable name="region" type="int*" length="N" />
		<Variable name="na" type="int*" length="N" />
		<Variable name="renorm" type="matrix*" length="N" />
    </Variables>

    <Tools>
        <!-- Remove the existing instances to ensure we are not repeating
        the job -->
		<Tool action="insert" after="Sort" type="set" name="cfd Reinit lambda" in="lambda" value="0.f"/>
		<Tool action="insert" after="Sort" type="set" name="cfd Reinit inormal" in="inormal" value="VEC_ZERO"/>
		<Tool action="insert" after="Sort" type="set" name="cfd Reinit frees" in="frees" value="0"/>
		<Tool action="insert" after="Sort" type="set" name="cfd Reinit region" in="region" value="0"/>
		<Tool action="insert" after="Sort" type="set" name="cfd Reinit na" in="na" value="0"/>
		<Tool action="insert" after="Sort" type="set" name="cfd Reinit dshepard" in="dshepard" value="VEC_ZERO"/>

        <Tool name="lambda" action="insert" after="*MLS_mls_fluid" type="kernel" entry_point="lambda" path="Detector.cl"/>
		<Tool name="first free surface detection" action="insert" after="lambda" type="kernel" entry_point="first" path="Detector.cl"/>
    	<Tool name="grad gamma" action="insert" before="lambda" type="kernel" path="DeltaShepard.cl"/>
		<Tool name="free surface normal" action="insert" after="first free surface detection" type="kernel" entry_point="normal" path="Detector.cl"/>
		<Tool name="free surface detector" action="insert" after="free surface normal" type="kernel" entry_point="second" path="Detector.cl"/>
		<Tool name="free surface region" action="insert" after="free surface detector" type="kernel" entry_point="region" path="Detector.cl"/>
		<!--Tool name="free surface normal corrected" action="insert" after="free surface region" type="kernel" entry_point="normalfs" path="Detector.cl"/-->
   </Tools>
</sphInput>
